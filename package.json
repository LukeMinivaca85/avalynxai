{
  "name": "ava-i",
  "version": "6.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "check": "node --check app.js && node --check server.mjs && node --check api/proxy.mjs && node --check server/proxy-core.mjs && node --check scripts/build-static.mjs",
    "render:build": "npm install && npm run check && npm run build"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {}
}
